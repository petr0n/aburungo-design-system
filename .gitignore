node_modules/
dist/*
# ...except the generated token sheet. colors_and_type.css imports it, so the
# 27 preview pages render unstyled from a fresh clone without it. They are
# static HTML served over http.server with no build step -- requiring a build
# first would defeat the point of having them.
!dist/tokens.plain.css
*.tsbuildinfo
.DS_Store


# Third-party design skills. Kept on disk, out of the repo — each is
# regenerated by its own installer, so tracking them would vendor ~6MB of
# someone else's source. Reinstall with:
#   npx impeccable install          -> .claude/skills/impeccable, .github/*
#   npx skills add <taste-skill>    -> .agents/skills (see skills-lock.json)
# frontend-design is the exception: hand-copied from the marketplace cache
# per plan task 0.5, so it is tracked.
.agents/
.claude/skills/*
!.claude/skills/frontend-design/
.github/agents/
.github/hooks/
.github/skills/

# Rendered design screenshots (scripts/shots.mjs)
scripts/.shots-out/

# Affinity Designer sources. Kept on disk, never pushed — they are multi-MB
# binaries git cannot diff, and the exported PNGs in assets/ are what ships.
# Repo-wide rather than assets/-only: docs/ has one too.
#
# The lock file needs its own line. This comment used to claim `*.afdesign`
# also covered `.afdesign~lock~`; it does not — the glob has to match to the
# end, and `samurai-kumon.afdesign~lock~` does not end in `.afdesign`. Two of
# them showed up untracked on 2026-08-21 while a crest was open in Affinity,
# one broad `git add` away from being committed.
*.afdesign
*.afdesign~lock~
