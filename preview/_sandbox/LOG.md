# Variant log

| Component | Variants | Shipped | Why |
|---|---|---|---|
| Card / surfaces | `surfaces-1-current.html` — before vs after | **After** | Page and card were both stone-0, so a card had no tonal separation from the page, while KanaGrid/KanaKeyboard/ScoreCard sat *darker* than it. Swapped `--color-bg` to stone-50 (page) and `--color-surface` to stone-0 (card); `Card` moved from `bg-bg` to `bg-surface`. Everything already on `bg-surface` became correct for free. |
| Focus ring | none — measured, not explored | `ring-offset-2 ring-offset-bg` | A ring on the control edge was 2.25:1 on the primary button and 1.03:1 on Akane. The offset puts page colour between control and ring so it reads against the page at 4.95:1. Enforced by `check-adherence.mjs`. |
| Vibrancy (A+B) | `vibrancy-1-before-after.html` | **After** | Every colour that landed on a surface was a 50/100 tint — `action-2-bg` was Rokushō at 97% white, `tag-bg` Ōgon at 95% white — while `bg-inverse` was never built at all. The drop's own index page uses the five at full strength on large fields. **A:** `AppHeader` and `KanaKeyboard` take the Sumi-iro band, Ōgon hairline, Akane mark. **B:** `action-2-bg` → Rokushō 100 with a Rokushō 500 border; `tag-bg` → Ōgon 200. Also reverted the values I had dulled for contrast: focus back to Ōgon 500, tag-fg to Ōgon 700, progress-fill to Rokushō 500. |
| KanaKeyboard | `kana-2-green.html` — 4 variants | **2 · Rokushō 700 frame, warm-paper keys** | Sumi-iro for both the header band and the keyboard put two near-black slabs on one screen. Deep green gives the second colour block without competing with the band, and paper keys keep the kana legible — reading them is the task. Rejected: 4 (Rokushō 500) too bright, competes with the header; 3 (green keys) keys blend into the frame; 1 (Sumi-iro) the original problem. **Deviates from v3**, which assigns the keyboard frame to Sumi-iro. |
| PhraseCard | `phrasecard-1-colour.html` (4), `-2-resolved.html` (3+banners), `-3-accents.html` | **accent as a prop** | Ai-iro on the Japanese and Rokushō on the reading — implements `--color-fg-heading`, which v3 assigns to "headings and JP content" and nothing used. The top rule was going to be locked to one colour; the palette author rejected that: colour should be flexible so it can carry meaning (scenario, unit, state). So `accent` is a prop over a set of the brand colours, defaulting to Ōgon. Rejected: inverted Ai-iro card (a third dark slab on the surface a learner stares at all session). |
| AnswerResult | `phrasecard-2-resolved.html` | **Akane/Rokushō 100 + 300 border** | Banners were 50-tints at ~95% white. Now tinted with a visible border and the glyph at full 500. Rejected: solid Akane band — reads as punishment, and the assessment principles put the gentleness in what happens next, not the volume of the verdict. |
| Crests 3–5 | `crest-3-4-5.html` (5 grounds × page/card + opacity sweep), `crest-measure.html` (bare stages, read by script) | **blossom at `--alpha .46`, two keyed silhouettes at `.4`** | Only three motifs are drawn, so five books had two grounds between them. Crest 3 is the blossom, laid out on crest-1/2's diagonal by `scripts/make-crest-tile.mjs`; 4 and 5 are the flat silhouettes already in `assets/` with the white keyed to alpha — the leaf and the clover again, in a different cut. Raw, the three measured 0.4468 / 0.3318 / 0.3318 against crest-2's 0.5879, so all three are brought to weight **in the art** rather than with a per-crest `--emboss-opacity`: the opacity knob is public and a call site can raise it where the contrast gate cannot see. Rejected: drawing two new kamon in SVG — the standing rule is to use the author's asset, never to hand-trace one. |
